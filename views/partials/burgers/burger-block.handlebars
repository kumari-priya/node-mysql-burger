<li class="list-group-item d-flex justify-content-between align-items-center">
	{{id}}. {{burger_name}}
{{#unless devoured}}
	<button class="btn btn-success btn-sm change-devoured" data-id="{{id}}" data-devoured="{{devoured}}">
		Devour it!
	</button>
	{{/unless}}
</li>
